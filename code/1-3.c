#include <stdio.h>
int count = 0;

int power(int base, int exp) {
    base * base;
    count++;
    power(b)
        if (count==)

}

int main() {
    printf("%d\n", power(2, 3));
    printf("%d\n", power(5, 0));
    printf("%d\n", power(3, 4));

    return 0;
}
